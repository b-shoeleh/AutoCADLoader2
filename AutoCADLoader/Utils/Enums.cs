namespace AutoCADLoader.Utils
{
    public enum Status
    {
        Matches,
        Outdated,
        DoesntExist,
        Newer
    }

    public enum ResourceType
    {
        Package,
        Setting,
        Support
    }
}
