namespace AutoCADLoader.Utility
{
    public enum Status
    {
        Matches,
        Outdated,
        DoesntExist,
        Newer
    }

    public enum ResourceType
    {
        Package,
        Setting,
        Support
    }
}
